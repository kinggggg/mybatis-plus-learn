spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mp-advance?useSSL=false&serverTimezone=GMT%2B8
    username: root
#    password: 123456789
    password: dd&me123

# mybatis-plus配置控制台打印完整带参数SQL语句
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 逻辑删除配置, 默认值也是1
      logic-delete-value: 1
      # 逻辑未删除配置, 默认值也是0
      logic-not-delete-value: 0
